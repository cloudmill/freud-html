mixin breadcrumbs 
  .breadcrumbs(class=attributes.class data-aos='fade-up') 
    ul.breadcrumbs__list 
      li.breadcrumbs__item 
        a.breadcrumbs__link(href="#" tabindex="0") 
          |Главная
      if (attributes.items)
        each item in attributes.items 
          li.breadcrumbs__item 
            a.breadcrumbs__link(href="#" tabindex="0") 
              |#{item}
      li.breadcrumbs__item 
        .breadcrumbs__txt 
          |#{attributes.page}